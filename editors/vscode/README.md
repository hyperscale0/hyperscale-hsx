# HSX editor grammar

This extension colors HSX programs and headers. It does not install the compiler,
report type errors or start a language server. Install the HSX package separately
and run `hsx check file.hsx` for compiler diagnostics.

Begin with the [language guide](../../docs/README.md). Syntax coloring does not
prove a valid program; the compiler checks bindings, types and UDL rules.

# weighted_distribution

This example applies the `weighted_distribution` standard-library module. Its sibling UDL file pins the canonical compiler output byte for byte. The company and parties are invented.
